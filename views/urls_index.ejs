<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
  </head>
  <body>
    <%- include partials/_header.ejs %>
    <% if(!signedIn) { %>
      <h2>Please login or register above</h2>
    <% } else { %>
      <ul>
        <% for(var obj in urls) {%>
          <li><a href = '/u/<%= obj %>'><%= obj %></a>: <%= urls[obj].url %>
          <a href = '/urls/<%= obj %>'>Edit</a>
          <form method="POST" action="/urls/<%= obj %>?_method=DELETE"><input type="submit" value="Delete"></form>
          </li>
        <% } %>
      </ul>
      <a href='/urls/new'>Shorten a new URL</a>
    <% } %>
  </body>
</html>